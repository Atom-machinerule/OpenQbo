sudo apt-get install ros-hydro-mjpeg-server

###  PATCH for running mjpgserver on HYDRO  ###

Add CvBridge.h to /opt/ros/hydro/include/cv_bridge

Use the command below, unless the associated file locations are different.



sudo mv /opt/ros/hydro/stacks/qbo_mjpeg_server/Dependencies/CvBridge.h /opt/ros/hydro/include/cv_bridge

